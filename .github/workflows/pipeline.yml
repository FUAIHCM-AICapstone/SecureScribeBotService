name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - master
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'LICENSE'
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:
    inputs:
      force_runtime_build:
        description: 'Force rebuild runtime image'
        required: false
        type: boolean
        default: false

env:
  DOCKER_USERNAME: luongnguyenminhan
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
  REGISTRY: luongnguyenminhan

jobs:
  # Stage 1: Check if runtime image exists
  check-runtime:
    name: Check Runtime Image
    runs-on: ubuntu-latest
    outputs:
      runtime-exists: ${{ steps.check.outputs.exists }}
      runtime-image: ${{ steps.image.outputs.runtime }}
    steps:
      - name: Set image name
        id: image
        run: |
          echo "runtime=luongnguyenminhan/securescribe:bot-runtime" >> $GITHUB_OUTPUT

      - name: 🐳 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ env.DOCKER_PASSWORD }}

      - name: ✅ Check if runtime image exists
        id: check
        run: |
          if docker pull luongnguyenminhan/securescribe:bot-runtime 2>/dev/null; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "✅ Runtime image already exists"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "⚠️ Runtime image does not exist, will build"
          fi

  # Stage 1b: Build Runtime Image (only if not exists or forced)
  build-runtime:
    name: Build Runtime Image
    runs-on: ubuntu-latest
    needs: check-runtime
    if: needs.check-runtime.outputs.runtime-exists == 'false' || github.event.inputs.force_runtime_build == 'true'
    outputs:
      runtime-image: ${{ needs.check-runtime.outputs.runtime-image }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🐳 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ env.DOCKER_PASSWORD }}

      - name: 🔨 Build and push runtime image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./build/Dockerfile.runtime
          push: true
          tags: |
            ${{ needs.check-runtime.outputs.runtime-image }}

      - name: ✅ Runtime image built successfully
        run: |
          echo "🎉 Runtime image: ${{ needs.check-runtime.outputs.runtime-image }}"

  # Stage 2: Build Backend Image
  build-backend:
    name: Build Backend Image
    runs-on: ubuntu-latest
    needs: check-runtime
    outputs:
      backend-image: ${{ steps.image.outputs.backend }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set image name
        id: image
        run: |
          echo "backend=luongnguyenminhan/securescribe:bot" >> $GITHUB_OUTPUT

      - name: 🐳 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ env.DOCKER_PASSWORD }}

      - name: 🔨 Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./build/Dockerfile.app
          push: true
          build-args: |
            RUNTIME_IMAGE=luongnguyenminhan/securescribe:bot-runtime
          tags: |
            ${{ steps.image.outputs.backend }}

  # Stage 5: Deploy (only on push to main/master)
  # Final: Summary
  pipeline-summary:
    name: Pipeline Summary
    runs-on: ubuntu-latest
    needs: [build-runtime, build-backend]
    if: always()
    steps:
      - name: 📊 Pipeline Summary
        run: |
          echo "=========================================="
          echo "✅ Pipeline Execution Summary"
          echo "=========================================="
          echo "Build Runtime: ${{ needs.build-runtime.result }}"
          echo "Build Backend: ${{ needs.build-backend.result }}"
          echo "=========================================="
